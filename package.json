{
  "author": "Maciej Laciak",
  "dependencies": {
    "colyseus.js": "~0.14"
  },
  "devDependencies": {
    "@colyseus/arena": "~0.14",
    "@colyseus/command": "~0.2",
    "@colyseus/loadtest": "~0.14",
    "@colyseus/monitor": "~0.14",
    "@colyseus/testing": "~0.14",
    "@types/cors": "^2",
    "@types/express": "^4",
    "@types/mocha": "^9",
    "colyseus": "~0.14",
    "copyfiles": "^2",
    "cors": "^2",
    "esbuild": "~0.14",
    "express": "^4",
    "mocha": "^9",
    "rimraf": "^2",
    "tailwindcss": "^3",
    "ts-node": "^10",
    "ts-node-dev": "^2",
    "typescript": "^3"
  },
  "license": "MIT",
  "name": "purescript-colyseus-client",
  "private": "true",
  "scripts": {
    "build": "spago build",
    "example:chat:client:build": "cd examples/chat/client && spago build",
    "example:chat:client:css": "cd examples/chat/client && tailwindcss --config tailwind.config.cjs --input src/css/styles.css --output static/index.generated.css",
    "example:chat:client:serve": "npm run example:chat:client:build && npm run example:chat:client:css && cd examples/chat/client && esbuild --bundle --outfile=static/index.js --servedir=static src/js/index.js",
    "example:chat:server:start": "cd examples/chat/server && npm start",
    "lint": "eslint .",
    "postinstall": "spago install",
    "server:start": "ts-node test/ts/test-server.ts",
    "server:stop": "kill $(ps -e | grep 'ts-node test/ts/test-server.ts' | grep -v grep | xargs | cut -d ' ' -f 1)",
    "test": "spago test"
  }
}
